Perhatiin dulu!

  @Autobind
  dragOverHandler(event: DragEvent): void {
    if (event.dataTransfer && event.dataTransfer.types[0] === 'text/plain') {
      event.preventDefault();
      const listEl = this.element.querySelector('ul')!;
      listEl.classList.add('droppable');
    }
  }

  @Autobind
  dragLeaveHandler(even: DragEvent): void {
    const listEl = this.element.querySelector('ul')!;
    listEl.classList.remove('droppable');
  }

  dropHandler(event: DragEvent): void {
    console.log(event.dataTransfer!.getData('text/plain'));
  }

Drop handler itu baru bisa jalan kalau di drag over handler nya kita kasih
event.preventDefault(); baru artinya di ngedrop gitu.

bukan bukan ... drag over handler perilaku default nya itu kayak membuka file gitu,
jadi ketika dia ngelepas drag nya maka event drop tidak tertrigger.

iya paham paham ...